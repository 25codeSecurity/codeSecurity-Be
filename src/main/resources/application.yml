spring:
  application:
    name: coin-futures
  datasource:
    url: jdbc:h2:mem:testdb  # ??? ??
    driver-class-name: org.h2.Driver
    username: sa
    password:
    h2:
      console:
        enabled: true          # ? ?? ??
        path: /h2-console
    jpa:
      hibernate:
        ddl-auto: update       # create, update, validate, none ? ?? ??
      show-sql: true           # ??? SQL ????
      properties:
        hibernate:
          format_sql: true
