spring:
  application:
    name: coin-futures

  datasource:
    url: jdbc:h2:file:E:/25codeSecurityBE/codeSecurity-Be/db/testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update  # create, update, validate, none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console